# Sistema de Gestão para Cachaçaria Antônio Carlos

**Para rodar o projeto pela primeira vez, instale as dependências abaixo:**
- npm install
- npm install express
- npm install cors

Para rodar o projeto utilize: `npm start`
